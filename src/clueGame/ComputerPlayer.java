package clueGame;
import java.util.*;

public class ComputerPlayer extends Player {
	BoardCell pickLocation(Set<BoardCell> targets) {
		BoardCell target = new BoardCell();
		// TODO: select room if in targets. otherwise random.
		return target;
	}
	void makeAccusation(){
		// TODO: implementation
	}
   /* createSuggestion() {
	*
	  }
    */
	
}
